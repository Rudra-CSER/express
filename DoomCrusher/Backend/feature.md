=<>  Auth
     => register 
     => login
     => logout (Token Black Listing )
     => LASt Otp based 

=<> post
     =>  create
     => can see the feed
     => like post (collection tpyes)
     => save post


=<> users 
    => followers 
    => 
    
